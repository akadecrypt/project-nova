# NOVA Backend Dependencies
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-dotenv==1.0.0
pydantic==2.5.3
pydantic-settings==2.1.0

# SQLite fix for older systems (must be before chromadb)
# pysqlite3-binary  # Only needed on Linux with SQLite < 3.35.0

# Vector Database
chromadb==0.4.22
# Pin posthog to avoid telemetry errors
posthog==3.0.2

# AI/ML
openai==1.12.0
langchain==0.1.6
langchain-community==0.0.19
sentence-transformers==2.3.1

# HTTP Client
httpx==0.26.0
aiohttp==3.9.3

# Utilities
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# CORS
starlette==0.35.1
